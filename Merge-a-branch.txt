### Checkout to main/master
git checkout main/master

### Select your branches
git branch

### Merge the branches with the main/master
git merge <branches name>

### Push to github repo
git push

### (Optional) Delete branch after merge
git branch -D <branches name>